<% content_for :title, "Thank You for Your Order - " + site_name %>

<div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div class="text-center">
      <!-- Success Icon -->
      <div class="mx-auto flex items-center justify-center h-24 w-24 rounded-full bg-green-100 mb-6">
        <i class="fas fa-check-circle text-4xl text-green-600"></i>
      </div>

      <!-- Success Message -->
      <h1 class="text-3xl font-bold text-green-700 mb-4">
        Thank You for Your Order!
      </h1>

      <p class="text-lg text-gray-600 mb-6">
        Insha Allah, we will send you your free Quran copy very soon.
      </p>

      <!-- Islamic Message -->
      <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
        <p class="text-green-800 font-medium">
          "Whoever guides [another] to something good has the reward of one who does it."
        </p>
        <p class="text-sm text-green-600 mt-2">
          - Prophet Muhammad (PBUH)
        </p>
      </div>

      <!-- What's Next Section -->
      <div class="text-left bg-white p-6 rounded-lg shadow-md mb-6">
        <h3 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
          <i class="fas fa-clock text-green-600 mr-2"></i>
          What's Next?
        </h3>
        <div class="space-y-3">
          <div class="flex items-start">
            <div class="text-green-600 mr-3 mt-1">
              <i class="fas fa-envelope"></i>
            </div>
            <div>
              <h4 class="font-medium text-gray-800">Email Confirmation</h4>
              <p class="text-sm text-gray-600">You'll receive a confirmation email with order details.</p>
            </div>
          </div>
          <div class="flex items-start">
            <div class="text-green-600 mr-3 mt-1">
              <i class="fas fa-truck"></i>
            </div>
            <div>
              <h4 class="font-medium text-gray-800">Processing</h4>
              <p class="text-sm text-gray-600">Your order will be processed within 3-5 business days.</p>
            </div>
          </div>
          <div class="flex items-start">
            <div class="text-green-600 mr-3 mt-1">
              <i class="fas fa-box-open"></i>
            </div>
            <div>
              <h4 class="font-medium text-gray-800">Shipping</h4>
              <p class="text-sm text-gray-600">Free international shipping to your location.</p>
            </div>
          </div>
          <div class="flex items-start">
            <div class="text-green-600 mr-3 mt-1">
              <i class="fas fa-prayer"></i>
            </div>
            <div>
              <h4 class="font-medium text-gray-800">Your Reward</h4>
              <p class="text-sm text-gray-600">You're helping spread the noble message of Islam.</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Action Buttons -->
<div class="space-y-4">
  <a href="<%= root_path %>"
     class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition">
    <i class="fas fa-home mr-2"></i>
    Return to Home
  </a>

  <!-- Share with Others Button -->
  <button onclick="openShareModal()"
     class="w-full flex justify-center py-3 px-4 border border-gray-300 rounded-lg shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition">
    <i class="fas fa-share-alt mr-2"></i>
    Share with Others
  </button>
</div>

<!-- Share Modal -->
<div id="shareModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden z-50">
  <div class="relative top-20 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
    <div class="mt-3 text-center">
      <h3 class="text-lg font-medium text-gray-900">Share Free Quran</h3>
      
      <div class="mt-4 space-y-3">
        <!-- Direct Order Link -->
        <a href="<%= new_order_path(quran_id: @quran_id) %>"
           class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-green-600 hover:bg-green-700">
          <i class="fas fa-copy mr-2"></i>
          Order Another Quran
        </a>

        <!-- Social Sharing -->
        <div class="flex justify-center space-x-2 py-2">
          <!-- WhatsApp -->
          <a href="https://wa.me/?text=<%= u("Get your free Quran from #{site_name}! #{root_url}orders/new#{@quran_id ? "?quran_id=#{@quran_id}" : ''}") %>"
             target="_blank"
             class="bg-green-500 text-white p-3 flex items-center justify-center rounded-full hover:bg-green-600">
            <i class="fab fa-whatsapp"></i>
          </a>
          
          <!-- Facebook -->
          <a href="https://www.facebook.com/sharer/sharer.php?u=<%= u("#{root_url}orders/new#{@quran_id ? "?quran_id=#{@quran_id}" : ''}") %>"
             target="_blank"
             class="bg-blue-600 text-white p-3 flex items-center justify-center rounded-full hover:bg-blue-700">
            <i class="fab fa-facebook-f"></i>
          </a>
          
          <!-- Twitter -->
          <a href="https://twitter.com/intent/tweet?text=<%= u("Get your free Quran! #{root_url}orders/new#{@quran_id ? "?quran_id=#{@quran_id}" : ''}") %>"
             target="_blank"
             class="bg-blue-400 text-white p-3 flex items-center justify-center rounded-full hover:bg-blue-500">
            <i class="fab fa-twitter"></i>
          </a>
          
          <!-- Email -->
          <a href="mailto:?subject=<%= site_name %>&body=Get your free Quran copy: <%= u("#{root_url}orders/new#{@quran_id ? "?quran_id=#{@quran_id}" : ''}") %>"
             class="bg-gray-600 text-white p-3 flex items-center justify-center rounded-full hover:bg-gray-700">
            <i class="fas fa-envelope"></i>
          </a>
        </div>

        <!-- Copy Link -->
        <button onclick="copyShareLink(this)"
           class="w-full flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
          <i class="fas fa-link mr-2"></i>
          Copy Share Link
        </button>
      </div>
      
      <div class="mt-4">
        <button onclick="closeShareModal()"
           class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-gray-700 bg-gray-100 hover:bg-gray-200">
          Close
        </button>
      </div>
    </div>
  </div>
</div>

      <% if @daily_order_info %>
    <p class="mt-2 text-sm">
      Daily orders: <%= @daily_order_info[:today_count] %> of <%= @daily_order_info[:daily_limit] %>
      (<%= @daily_order_info[:remaining] %> remaining today)
    </p>
  <% end %>

      <!-- Contact Info -->
      <div class="mt-8 text-sm text-gray-500">
        <p>
          <i class="fas fa-envelope mr-1"></i>
          Questions? Contact us at contact@freequran.org
        </p>
        <p class="mt-2">
          <i class="fas fa-globe mr-1"></i>
          Serving Muslims worldwide since 2025
        </p>
      </div>
    </div>
  </div>
</div>

<% content_for :page_scripts do %>
<script>
// For Option 3 - Modal
function openShareModal() {
  document.getElementById('shareModal').classList.remove('hidden');
}

function closeShareModal() {
  document.getElementById('shareModal').classList.add('hidden');
}

function copyShareLink(button) {
  const shareUrl = '<%= @quran_id.present? ? "#{root_url}orders/new?quran_id=#{@quran_id}" : "#{root_url}orders/new" %>';
  
  // Store original button content
  const originalContent = button.innerHTML;
  
  navigator.clipboard.writeText(shareUrl).then(function() {
    // Success feedback
    button.innerHTML = '<i class="fas fa-check mr-2"></i>Share Link Copied!';
    button.classList.remove('bg-white', 'text-gray-700', 'hover:bg-gray-50');
    button.classList.add('bg-green-100', 'text-green-700', 'border-green-300');
    
    // Revert after 2 seconds
    setTimeout(() => {
      button.innerHTML = originalContent;
      button.classList.remove('bg-green-100', 'text-green-700', 'border-green-300');
      button.classList.add('bg-white', 'text-gray-700', 'hover:bg-gray-50');
    }, 2000);
    
  }).catch(function(err) {
    console.error('Could not copy text: ', err);
    
    // Fallback for older browsers
    const tempInput = document.createElement('input');
    tempInput.value = shareUrl;
    document.body.appendChild(tempInput);
    tempInput.select();
    tempInput.setSelectionRange(0, 99999); // For mobile devices
    
    try {
      const successful = document.execCommand('copy');
      if (successful) {
        // Success feedback for fallback
        button.innerHTML = '<i class="fas fa-check mr-2"></i>Share Link Copied!';
        button.classList.remove('bg-white', 'text-gray-700', 'hover:bg-gray-50');
        button.classList.add('bg-green-100', 'text-green-700', 'border-green-300');
        
        setTimeout(() => {
          button.innerHTML = originalContent;
          button.classList.remove('bg-green-100', 'text-green-700', 'border-green-300');
          button.classList.add('bg-white', 'text-gray-700', 'hover:bg-gray-50');
        }, 2000);
      } else {
        throw new Error('Fallback copy failed');
      }
    } catch (fallbackErr) {
      console.error('Fallback copy failed: ', fallbackErr);
      // Error feedback
      button.innerHTML = '<i class="fas fa-times mr-2"></i>Failed to copy';
      button.classList.remove('bg-white', 'text-gray-700', 'hover:bg-gray-50');
      button.classList.add('bg-red-100', 'text-red-700', 'border-red-300');
      
      setTimeout(() => {
        button.innerHTML = originalContent;
        button.classList.remove('bg-red-100', 'text-red-700', 'border-red-300');
        button.classList.add('bg-white', 'text-gray-700', 'hover:bg-gray-50');
      }, 2000);
    } finally {
      document.body.removeChild(tempInput);
    }
  });
}

// Close modal when clicking outside
window.onclick = function(event) {
  const modal = document.getElementById('shareModal');
  if (event.target == modal) {
    closeShareModal();
  }
}
</script>
<% end %>