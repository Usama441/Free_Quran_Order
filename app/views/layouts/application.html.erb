<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= content_for?(:title) ? yield(:title) : "Free Quran Distribution" %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  
  <!-- Additional scripts for specific pages -->
  <% if content_for?(:head_scripts) %>
    <%= yield(:head_scripts) %>
  <% end %>
</head>

<body class="bg-gray-50 text-gray-900 font-sans">
  <!-- Header -->
  <%= render 'extras/header' %>

  <!-- Flash Messages Container -->
  <div id="flash-messages" class="max-w-6xl mx-auto mt-4 px-6 hidden">
    <div id="flash-container"></div>
  </div>

  <!-- app/views/layouts/application.html.erb -->
<% if maintenance_mode? %>
  <div class="bg-red-600 text-white text-center py-2 px-4">
    <div class="container mx-auto flex items-center justify-center">
      <i class="fas fa-tools mr-2"></i>
      <span class="font-semibold">System Maintenance in Progress</span>
      <span class="ml-4 text-sm opacity-90">Some features may be temporarily unavailable</span>
    </div>
  </div>
<% end %>

<% if debug_mode? %>
  <div class="bg-purple-600 text-white text-center py-1 px-4 text-sm">
    <div class="container mx-auto flex items-center justify-center">
      <i class="fas fa-bug mr-2"></i>
      <span>DEBUG MODE ACTIVE - Detailed logging enabled</span>
    </div>
  </div>
<% end %>

  <main class="min-h-screen">
    <%= yield %>
  </main>

  <!-- Footer -->
  <%= render 'extras/footer' %>

  <!-- Page-specific scripts -->
  <% if content_for?(:page_scripts) %>
    <%= yield(:page_scripts) %>
  <% end %>
</body>
</html>